import RegisterForm from "@/components/register/RegisterForm";

export const metadata = {
	title: "Register - Vorix",
	description: "Create a new Vorix account",
};

export default function RegisterPage() {
	return <RegisterForm />;
}
